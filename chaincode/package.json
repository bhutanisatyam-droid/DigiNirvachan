{
    "name": "evoting-chaincode",
    "version": "1.0.0",
    "description": "Hyperledger Fabric Chaincode for Secure E-Voting System",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc",
        "start": "fabric-chaincode-node start --chaincode-id-name $CORE_CHAINCODE_ID_NAME",
        "lint": "eslint src --ext .ts"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "fabric-contract-api": "^2.5.0",
        "fabric-shim": "^2.5.0"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "typescript": "^5.0.0"
    }
}